#!/usr/bin/env node
// Axstack setup CLI stub (pre-implementation).
// Prints usage and exits non-zero; real behavior lands after failing tests.
console.error('axstack: not implemented');
process.exit(1);
