{
  "version": 1,
  "note": "Raw relay discovery policy scenario. Assertions validate the declared contract and shipped prompt coverage; they are not model-behavior evidence.",
  "cases": [
    {
      "id": "relay-discovery-fallbacks",
      "title": "Optional one-way relay fails closed to the current Orca conversation",
      "skill_ref": "axstack-relay",
      "input": {
        "policy_absent": "An automated routine update has no Notification policy or explicit send authorization.",
        "cli_missing": "The shell command lookup cannot locate the hermes CLI.",
        "target_missing": "The target listing does not show the intended Telegram recipient.",
        "delivery_failed": "hermes send exits non-zero.",
        "delivery_uncertain": "hermes send times out or returns JSON without a message_id.",
        "reply_received": "The user replies on Telegram to a delivered message."
      },
      "expected": {
        "policy_absent": "Do not use relay.",
        "cli_missing": "Report relay unavailable in the current Orca conversation.",
        "target_missing": "Report relay not configured on this host and use the current-conversation fallback.",
        "delivery_failed": "Record the failed attempt, deliver through the current-conversation fallback, and leave every hold open.",
        "delivery_uncertain": "Record the attempt as uncertain and never resend it automatically.",
        "reply_received": "The reply is not routed to the driver and is never a receipt or authority; the user acts in the Orca conversation, worktree, or GitHub."
      }
    }
  ]
}
