{
  "version": 1,
  "evidence": "Contract decision scenarios, not live Orca cleanup proof",
  "cases": [
    {"id":"author-retained-while-open","input":"An author's Dispatch is accepted while its PR remains open.","expected":"Retain its session and worktree for repairs until merge or closure.","contractPattern":"author worktree and session.{0,100}until the PR merges or closes"},
    {"id":"reviewer-removed-at-acceptance","input":"A reviewer's Dispatch is accepted and its evidence is durable.","expected":"Release its terminal, verify closure, and remove its worktree.","contractPattern":"intermediate completion.{0,250}removes its worktree"},
    {"id":"dirty-non-author-salvaged","input":"A completed non-author worktree has uncommitted source and no protection hold.","expected":"Commit on a salvage ref, bundle, verify, then remove.","contractPattern":"git add -A.{0,220}git bundle verify.{0,180}removing"},
    {"id":"failed-bundle-verify","input":"The salvage bundle fails git bundle verify.","expected":"Hold the worktree.","contractPattern":"failed verify.{0,100}hold"},
    {"id":"ignored-non-cache-file","input":"A completed worktree has an ignored file that is not a known build or dependency cache.","expected":"Hold instead of salvaging.","contractPattern":"ignored non-cache.{0,150}hold"},
    {"id":"genuine-user-takeover","input":"Native ownership confirms a genuine user_takeover terminal.","expected":"List and preserve the terminal.","contractPattern":"genuine.{0,35}`user_takeover`.{0,100}never removed"},
    {"id":"repair-labelled-takeover","input":"An author repair terminal is labelled user_takeover and the run record has its exact repair Dispatch ID.","expected":"Treat it as owned for settlement; otherwise hold.","contractPattern":"repair Dispatch ID.{0,80}otherwise hold"},
    {"id":"unreachable-host","input":"Orca is unreachable during driver-start sweep.","expected":"Report one line and continue; hold only host items.","contractPattern":"Orca is unreachable.{0,110}continue.{0,110}unreachable host"},
    {"id":"terminal-absent-native-list","input":"A worker release result is release_unknown but native inspection and fresh terminal list show it gone.","expected":"Record the reconciliation and proceed.","contractPattern":"release_unknown.{0,130}fresh.{0,20}terminal list.{0,120}proceed"},
    {"id":"cross-repo-worktree","input":"The run record lists a settled Axstack worktree in another repository.","expected":"Include it in sweep and remove when all guards pass.","contractPattern":"per-run worktrees in other[\\s\\S]{0,25}repositories[\\s\\S]{0,650}remove"},
    {"id":"remote-branch-counterpart","input":"A settled worktree branch has a remote counterpart.","expected":"Keep the branch.","contractPattern":"remote counterpart are never deleted"},
    {"id":"other-run-reviewer","input":"A reviewer of another live Axstack run is settled, released, quiet for 60 minutes, and its evidence is durable.","expected":"Remove the settled reviewer worktree even while its owning run remains live.","contractPattern":"live owning run does not protect its settled.{0,25}reviewer worktree.{0,400}ANY Axstack run on this host"},
    {"id":"other-run-merged-author","input":"An author of another live Axstack run has a merged PR, retrievable head, and no active Dispatch for 60 minutes.","expected":"Remove the merged author worktree after preservation checks.","contractPattern":"live owning run does not protect its settled.{0,60}merged author after preservation checks.{0,400}ANY Axstack run on this host"},
    {"id":"adviser-between-rounds","input":"An Align or Spec adviser is being reused between rounds; its owning phase is still open and it spoke within 60 minutes.","expected":"Keep the adviser session until phase approval or stop.","contractPattern":"Align/Spec adviser.{0,180}phase approves or stops"},
    {"id":"release-unknown-shared-terminal","input":"A settled worker in a shared driver worktree has release_unknown, no working agent, and no terminal output for 60 minutes.","expected":"Close its exact terminal, using the guarded native fallback if needed.","contractPattern":"For `release_unknown`[\\s\\S]{0,300}otherwise hold unless inspection proves a settled, quiet worker[\\s\\S]{0,160}exact terminal[\\s\\S]{0,80}Unknown liveness holds"},
    {"id":"driver-terminal-protected","input":"A settled worker shares the driver's worktree; its terminal is quiet for 60 minutes, but the driver's terminal remains active.","expected":"Close only the worker terminal and never the driver terminal.","contractPattern":"shared or driver worktree[\\s\\S]{0,300}Never close the live coordinator[\\s\\S]{0,80}driver's own terminal"}
  ]
}
