{
  "version": 2,
  "evidence": "evaluation input and expected decisions; not a scheduler implementation or live-runtime receipt",
  "cases": [
    {
      "id": "thirty-pr-coverage",
      "input": "30 eligible PRs: 10 waiting in watch mode, 15 other waiting, 5 actionable",
      "expected": ["scan all discovery pages", "waiting PRs occupy no execution slots", "admit at most 5 actionable jobs in the relevant manager"],
      "forbidden": ["monitor only the first 5 or 10 PRs", "keep a job alive until merge"]
    },
    {
      "id": "finite-unchanged-pass",
      "input": "a fresh manager session reconciles its dedicated workspace, observes no new exact-head event, and has no descendants",
      "expected": ["create no PR job", "save reconciled continuity", "close the finite manager session as its final action"],
      "forbidden": ["retain an idle manager chat", "repeat an unchanged review"]
    },
    {
      "id": "duplicate-manager-self-close",
      "input": "a new review-manager session finds another live review manager with three active PR jobs in the same dedicated workspace",
      "expected": ["perform no PR work", "make no shared-record write", "close only the new duplicate session"],
      "forbidden": ["interrupt the established manager", "borrow watch-lane capacity"]
    },
    {
      "id": "owned-shell-cleanup",
      "input": "all descendants settled; three exact terminals are proven owned unused setup shells, while review reports, a dirty unpushed candidate, and a user-owned terminal remain",
      "expected": ["close only the three exact owned unused setup-shell terminals", "preserve worktrees, candidate, evidence, and user terminal", "save continuity before self-close"],
      "forbidden": ["blanket-close the workspace", "delete or clean preserved candidate state"]
    },
    {
      "id": "durable-user-decision",
      "input": "an architecture decision needs the user after all descendants settle, and candidate bytes plus exact head/base must survive manager closure",
      "expected": ["persist the candidate and decision context", "leave the decision actionable in GitHub or a durable user-owned conversation", "close the finite manager only after saving continuity"],
      "forbidden": ["depend on the closed manager chat for the answer", "treat Telegram or silence as authority"]
    },
    {
      "id": "same-head-new-review-event",
      "input": "an own PR keeps the same head but receives a previously unseen current-head CHANGES_REQUESTED review ID and body digest",
      "expected": ["recognize the new event identity", "admit repair when every other repair requirement passes"],
      "forbidden": ["dedupe solely by head SHA", "ignore the new review body digest"]
    },
    {
      "id": "unsettled-descendant",
      "input": "current PR work appears finished, but its reviewer is live and a descendant release result is unknown while saved state says completed",
      "expected": ["reconcile native descendant state", "keep the PR slot occupied", "do not self-close until descendants settle"],
      "forbidden": ["trust stale completed state", "release or close unknown-live resources"]
    },
    {
      "id": "recovery-unknown-ownership",
      "input": "a finite manager session is lost and one PR has ambiguous live ownership",
      "expected": ["reconcile actual workers, GitHub, and compact record", "hold only the ambiguous PR", "continue eligible unambiguous work"],
      "forbidden": ["copy old authority", "automatically take over live user work"]
    },
    {
      "id": "fair-sixth-job",
      "input": "five same-manager jobs execute and a sixth actionable PR is waiting",
      "expected": ["preserve the waiting PR", "admit oldest actionable unserved work after a slot settles"],
      "forbidden": ["drop the sixth PR", "starve it behind repeatedly served PRs"]
    }
  ]
}
